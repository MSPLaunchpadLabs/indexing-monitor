# Secrets and credentials — never commit
.env
service-account*.json
credentials*.json
*-credentials.json
google-credentials*.json
.streamlit/secrets.toml

# Local databases — recreated by the tool (one per client)
indexing.db
indexing.db-journal
data/


# Generated reports (per-client subfolders hold dated CSVs — all regenerated by the tool)
reports/**/*.csv

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Ephemeral deployment scratch
FORWARD_TO_TEAMMATE.md

.vercel
