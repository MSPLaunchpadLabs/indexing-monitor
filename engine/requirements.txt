# engine/ deps — used by the GitHub Actions runner. Leaner than the root
# requirements.txt (no Streamlit/pandas) so CI installs fast.
google-api-python-client==2.149.0
google-auth==2.35.0
requests==2.32.3
lxml>=5.3
